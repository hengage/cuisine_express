<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/layout.css">
</head>
<body>
    <header>
        <div class="container">

            <h3><a href="/">Cuisine Express</a></h3>

            <nav>
                <ul>
                    <% if (loggedIn) { %>
                        Logged in as 

                        <!-- Check if logged in user is a private user or business -->
                        <% if (currentUser.zipCode) { %>
                            <li>
                                <a href="<%= `/restaurant/dashboard/${currentUser.name}` %>">
                                    <%= currentUser.name %>
                                </a>
                            </li>
                        <% } else { %>
                            <li>
                                <a href="<%= `/users/${currentUser._id}` %>">
                                    <%= currentUser.fullName %>
                                </a>
                            </li>
                        <% } %>

                        <li>
                            <a href="/users/logout">Log out</a>
                    <%} else {%>
                        </li>

                        <li>
                            <a href="/login">Login</a>
                        </li>

                        <li>
                            <a href="/register">Register</a>
                        </li>
                    <% } %>

                    <li>
                        <a href="/restaurant/all">All restaurants</a>
                    </li>
                </ul>
            </nav>
        </div>
    </header>


    <div class="flash-message">
        <% if (flashMessages) { %>
            <%  if (flashMessages.success) { %>
                <div class="success-message" style="color: rgb(22, 236, 22);"><%= flashMessages.success %></div>
            <%  } else if (flashMessages.error) { %>
                <div style="color: red;" class="error-message"><%= flashMessages.error %></div>
            <% } %>
        <% } %>
    </div>

    <%- body %>

    <footer>
        FOOTER
    </footer>
    
</body>
</html>